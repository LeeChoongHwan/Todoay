<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Todoay" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
<!--        <item name="colorPrimaryVariant">@color/purple_700</item>-->
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorControlActivated">@color/main_color</item>
        <item name="bottomSheetDialogTheme">@style/CustomBottomSheetDialog</item>
        <item name="dialogTheme">@style/CustomDialog</item>
        <item name="android:datePickerDialogTheme">@style/MySpinnerDatePickerStyle</item>
    </style>

    <style name="customEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:colorControlActivated">@color/main_color</item>
        <item name="android:textColorLink">@color/main_color</item>
        <item name="android:textColorHighlight">@color/main_color</item>
    </style>

    <!-- BottomSheetDialog Style -->
    <style name="CustomBottomSheetDialog"
        parent="@style/ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheet</item>
        <item name="android:colorControlActivated">@color/main_color</item>
        <item name="android:textColorLink">@color/main_color</item>
        <item name="android:textColorHighlight">@color/main_color</item>
    </style>
    <style name="CustomBottomSheet" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="backgroundTint">@android:color/transparent</item>
    </style>

    <!-- AlertDialog Style -->
    <style name="CustomDialog" parent="@style/Theme.MaterialComponents.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="backgroundTint">@android:color/transparent</item>
    </style>

    <!-- DatePicker Style -->
    <style name="MySpinnerDatePickerStyle" parent="ThemeOverlay.Material3.Dialog">
        <!-- 상단 색 변경 -->
        <item name="colorAccent">@color/main_color</item>
        <!-- 선택한 날짜를 표시할 색상 -->
        <item name="colorControlActivated">@color/main_color</item>
        <item name="colorControlHighlight">@color/main_color</item>
        <item name="android:buttonBarButtonStyle">@style/DatePicker.ButtonStyle</item>
    </style>
    <style name="DatePicker.ButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/main_color</item>
    </style>

    <!-- NumberPicker Style -->
    <style name="CustomNumberPicker">
        <item name="android:textSize">15sp</item>
        <item name="fontFamily">@font/white</item>
    </style>

</resources>